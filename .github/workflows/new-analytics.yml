      - name: Run Pipeline
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
          # This forces Python to look in the current directory for modules
          PYTHONPATH: ${{ github.workspace }}
        run: python main.py
