      - name: Run Pipeline
        env:
          # This tells Python to look in the root directory for modules
          PYTHONPATH: ${{ github.workspace }}
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
        run: |
          python main.py
