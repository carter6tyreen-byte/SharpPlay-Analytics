name: SharpPLAY Analytics Pipeline

# THIS IS THE PART THAT WAS MISSING OR FORMATTED INCORRECTLY
on:
  schedule:
    - cron: '0 8 * * *'
  push:
    branches:
      - main
  workflow_dispatch:  # Added this so you can run it manually in the UI

jobs:
  update-dashboards:
    # ... rest of your jobs ...
