      - name: Run Pipeline
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          echo "Listing engine files:"
          ls -F | grep engine
          python main.py
