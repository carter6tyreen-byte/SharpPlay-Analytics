name: update-stats

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-analytics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install requests pandas numpy

      - name: Run Analytics Generation Script
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python generate_static_report.py

      - name: Commit and push updates
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # Only add if the script successfully created the file
          if [ -f "data/player_distributions.json" ]; then
            git add data/player_distributions.json
            git diff --quiet && git diff --staged --quiet || (git commit -m "Update player distributions [skip ci]" && git push)
          else
            echo "File data/player_distributions.json not found, skipping commit."
          fi
