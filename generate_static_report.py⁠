from analytics.machine_engine import AnalyticsEngine

def main():
    engine = AnalyticsEngine()
    
    # 1. Fetch both datasets
    pitcher_df = engine.get_pitcher_data()
    batter_df = engine.get_batter_data()
    
    # 2. Process and save to your index/dashboard
    # Example: Writing both tables into your HTML or JSON storage
    # This matches your existing logic but handles both objects
    save_to_dashboard(pitcher_df, batter_df)

if __name__ == "__main__":
    main()
